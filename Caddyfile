# Пока работаем по IP (без домена)
:80 {
    # Раздаем статические файлы из папки /srv внутри контейнера
    handle / {
        root * /srv
        file_server
    }

    # Когда будет app1
    # handle /app1/* {
    #     uri strip_prefix /app1
    #     reverse_proxy app1:8000
    # }

    log {
        output stdout
    }
}

# Когда появится домен
# demo1.mydomen.ru {
#     reverse_proxy demo1:8000
# }
